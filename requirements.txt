fastapi==0.141.1
uvicorn==0.48.0
pydantic==2.11.7
pydantic-settings==2.14.1
httpx==0.28.1

# --- added for the AI / decision engine (app/engine) ---
langchain==0.3.27
langgraph==0.6.7
# Groq, not Anthropic -- if this exact pin isn't resolvable, `pip install
# langchain-groq` and re-pin to whatever it resolves; the only requirement
# is compatibility with langchain>=0.3.
langchain-groq==0.2.3
sqlalchemy==2.0.43
pytest==8.4.2
python-dotenv==1.1.1